
  # Homestay Management System UI (Copy)

  This is a code bundle for Homestay Management System UI (Copy). The original project is available at https://www.figma.com/design/8IuLeApjeGzEnJ8oDk4wEG/Homestay-Management-System-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
